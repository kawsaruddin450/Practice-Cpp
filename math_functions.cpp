#include<iostream>
#include<cmath>
using namespace std;

int main(){
    int x = 10;
    int y = 5;

    // cout << max(x, y);
    // cout << pow(2, 3);
    // cout << sqrt(9);
    // cout << abs(-4);
    int n;
    cin >> n;
    cout << pow(n, 2);
    return 0;
}